import { SortByFrequencyPipe } from './sort-by-frequency.pipe';

describe('SortByFrequencyPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByFrequencyPipe();
    expect(pipe).toBeTruthy();
  });
});
